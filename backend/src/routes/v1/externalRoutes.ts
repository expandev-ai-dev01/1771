import { Router } from 'express';

const router = Router();

// PLACEHOLDER: Public routes (e.g., login, register, password reset) will be added here.
// router.use('/auth', authRoutes);

export default router;
